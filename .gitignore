
# Dependency directories (용량이 크고 설치로 생성 가능)
node_modules/
jspm_packages/

# Next.js build and production output (빌드 결과물)
.next/
out/
build/
dist/

# Environment variables (API 키 등 보안 정보 - 절대 업로드 금지)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs (로그 파일)
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Operating System files (OS 설정 파일)
.DS_Store
Thumbs.db

# Vercel related (Vercel 설정)
.vercel

# Testing (테스트 결과)
coverage/
